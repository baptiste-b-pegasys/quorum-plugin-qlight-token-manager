syntax = "proto3";

package proto;

option go_package = "./proto";
option java_package = "com.quorum.plugin.proto";
option java_outer_classname = "HelloWorld";

message PluginQLightTokenManager {
    message Request {
        string server = 1;
    }
    message Response {
        string token = 1;
    }
}

service PluginQLightTokenRefresher {
    rpc TokenRefresh(PluginQLightTokenManager.Request) returns (PluginQLightTokenManager.Response);
}